{
  "$schema": "../node_modules/@tauri-apps/cli/schema/acl/capability.schema.json",
  "identifier": "default",
  "description": "Permisos mínimos — solo spawning del sidecar Axum. Sin shell:allow-open (no URLs externas desde WebView).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-spawn",
    "shell:allow-execute"
  ]
}
